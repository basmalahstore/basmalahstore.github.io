$(document).ready(function() {
    $('.home__img-bg').slick({
        // dots: true,
        infinite: true,
        // speed: 500,
        fade: true,
        cssEase: 'linear',
        arrows: false,
        pauseOnFocus: false,
        autoplay: true,
        autoplaySpeed: 2000,

    });
});